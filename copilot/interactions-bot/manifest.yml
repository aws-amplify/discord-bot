# The manifest for the "interactions-bot" service.
# Read the full specification for the "Request-Driven Web Service" type at:
# https://aws.github.io/copilot-cli/docs/manifest/rd-web-service/

# Your service name will be used in naming your resources like log groups, App Runner services, etc.
name: interactions-bot
# The "architecture" of the service you're running.
type: Request-Driven Web Service

image:
  # Docker build arguments.
  # For additional overrides: https://aws.github.io/copilot-cli/docs/manifest/rd-web-service/#image-build
  build: Dockerfile
  # Port exposed through your container to route traffic to it.
  port: 3000

# http:
#   healthcheck:
#     path: /
#     healthy_threshold: 3
#     unhealthy_threshold: 5
#     interval: 10s
#     timeout: 5s

# Number of CPU units for the task.
cpu: 1024
# Amount of memory in MiB used by the task.
memory: 2048

# Optional fields for more advanced use-cases.
#
# variables:                    # Pass environment variables as key value pairs.
#   LOG_LEVEL: info
#
# tags:                         # Pass tags as key value pairs.
#   project: project-name

# You can override any of the values defined above by environment.
# environments:
#   test:
#     variables:
#       LOG_LEVEL: debug        # Log level for the "test" environment.

variables:
  DISCORD_TOKEN: /copilot/amplify-discord/main/secrets/DISCORD_TOKEN
  DISCORD_PUBLIC_KEY: /copilot/amplify-discord/main/secrets/DISCORD_PUBLIC_KEY
  DISCORD_APP_ID: /copilot/amplify-discord/main/secrets/DISCORD_APP_ID

environments:
  main:
    variables:
      DISCORD_TOKEN: /copilot/amplify-discord/main/secrets/DISCORD_TOKEN
      DISCORD_PUBLIC_KEY: /copilot/amplify-discord/main/secrets/DISCORD_PUBLIC_KEY
      DISCORD_APP_ID: /copilot/amplify-discord/main/secrets/DISCORD_APP_ID
  next:
    variables:
      DISCORD_APP_ID: /copilot/amplify-discord/next/secrets/DISCORD_APP_ID
      DISCORD_PUBLIC_KEY: /copilot/amplify-discord/next/secrets/DISCORD_PUBLIC_KEY
      DISCORD_TOKEN: /copilot/amplify-discord/next/secrets/DISCORD_TOKEN
